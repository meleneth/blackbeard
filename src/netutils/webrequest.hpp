#ifndef ___webrequest_inc
#define ___webrequest_inc 1

#include "SDL_types.h"

class WebRequest {
    public:
        WebRequest(int bar);
        ~WebRequest();

        int Foo(void);

    private:
    protected:
};

#endif
