#ifndef ___netjob_inc
#define ___netjob_inc 1

#include"SDL_types.h"

class NetJob {
    public:
        NetJob(int bar);
        ~NetJob();

        int Foo(void);

    private:
    protected:
};

#endif
